spring.application.name=smartcity-backend

# Database Configuration
spring.datasource.url=jdbc:postgresql://localhost:5432/smartcity_db
spring.datasource.username=postgres
spring.datasource.password=1q2w3e
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true

# Server Port
server.port=8080

# =========== EMAIL CONFIGURATION ===========
# Gmail SMTP Configuration (TRY BOTH OPTIONS)

# OPTION 1: Port 587 with STARTTLS (Recommended)
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=smartcityinquiries@gmail.com
spring.mail.password=sjnuuevwecfgncjz

spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.starttls.required=true

# FIX: Remove or fix the problematic TLS protocol line
# Use one of these formats:
# spring.mail.properties.mail.smtp.ssl.protocols=TLSv1.2
# OR remove it completely

# Connection settings
spring.mail.properties.mail.smtp.connectiontimeout=5000
spring.mail.properties.mail.smtp.timeout=5000
spring.mail.properties.mail.smtp.writetimeout=5000

# Trust all certificates for testing (remove in production)
spring.mail.properties.mail.smtp.ssl.trust=*

# Debugging
spring.mail.properties.mail.debug=true
logging.level.org.springframework.mail=DEBUG
logging.level.com.sun.mail=DEBUG

# Application Settings
spring.main.lazy-initialization=true